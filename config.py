# config.py

import torch

# --- Основные пути ---
# Путь к папке с данными
DATA_DIR = './data'
# Путь, куда мы сохраним нашу лучшую модель
MODEL_SAVE_PATH = './best_calorie_predictor.pth'

# --- Параметры обучения (гиперпараметры) ---
# Устройство для обучения: 'cuda' (GPU) или 'cpu' (CPU)
# Код автоматически выберет GPU, если он доступен
DEVICE = "cuda" if torch.cuda.is_available() else "cpu"

# Размер батча: сколько примеров модель видит за один шаг
BATCH_SIZE = 32
# Количество эпох: сколько раз мы "прогоним" все данные через модель
EPOCHS = 10
# Скорость обучения (learning rate): как сильно модель обновляет свои веса
LEARNING_RATE = 0.001

# --- Настройки модели ---
# Размерность вектора для каждого ингредиента
EMBEDDING_DIM = 128

# --- Воспроизводимость ---
# Фиксированное число для инициализации генераторов случайных чисел
# Это нужно, чтобы при каждом запуске получать одинаковые результаты
SEED = 42